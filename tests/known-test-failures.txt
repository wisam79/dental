# Current baseline of failing tests.
# CI in non-strict mode allows only these failures.
#
# All tests previously listed here have been FIXED:
#   - MathTests.CalculateIoU_* (3 tests)     → Fixed: added CalculateIoU wrapper in OnnxInferenceService
#   - BiometricServiceTests.*                 → Fixed: corrected UniquenessScore normalization formula
#   - ForensicRulesEngineTests.*              → Fixed: re-added Crown+Filling Observation rule
#   - AnalysisLabViewModelTests.*  (2 tests)  → Fixed: changed mock from float to double (matching interface)
#   - EncryptionServiceTests.*                → Fixed: implemented TryLegacyDecrypt with actual AES-CBC
#   - AuthServiceTests.LoginAsync_* (2 tests) → Fixed: mocked FirstOrDefaultAsync instead of GetAllAsync
#
# Intermittent in local/CI (timing-sensitive, not a logic bug):
DentalID.Tests.ViewModels.SubjectsViewModelTests.SaveSubject_ShouldHandleErrors_Gracefully
